import Allele from './genetics-model.js';

describe('Allele', function () {
  it('has a getter name', function () {
    let a = new Allele("blue");
    assert.equal('xyz', 'bar', 'foo equal `bar`');
  });
});